---
- name: Add Splunk index
  hosts: "{{ target_node }}"
  become: yes
  vars:
    index_name: ""
    maxDataSizeMB: ""
    datatype: ""
    file_path: ""
  tasks:
    - name: Ensure indexes.conf file exists
      file:
        path: "{{ file_path }}"
        state: touch
        owner: splunk
        group: splunk
        mode: "0644"

    - name: Add or update index stanza
      ini_file:
        path: "{{ file_path }}"
        section: "{{ index_name }}"
        option:
          maxDataSizeMB: "{{ maxDataSizeMB }}"
          datatype: "{{ datatype }}"
        owner: splunk
        group: splunk
        mode: "0644"
